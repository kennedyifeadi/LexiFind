import { motion } from "framer-motion"

export const Bookmarks = () =>{

    return(
        <motion.div 
        className="md:h-full md:w-full flex flex-col"
        initial={{opacity:0}}
        animate={{opacity:1}}
        exit={{opacity:0}}
        >
            kjdsfdlvklfdvdf
        </motion.div>
    )
}